# 孩子端 UI 重构实现计划

> **For agentic workers:** REQUIRED SUB-LEVEL SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把孩子端 `/play` 整体 UI 从暗色星球太空主题升级为明亮扁平游戏关卡风，新增底部 Tab 导航、首页两侧快捷入口、营地背景动画和学科动态色按钮，并统一所有子页面视觉风格。

**Architecture：** 通过新增可复用视觉组件（`AvatarBadge`、`StatusCapsule`、`BottomTabBar`、`IconButton`、`ChapterNode`、`ProfileModal`）承载新设计语言；在 `PlayLayout` 中统一背景与底部 Tab；`PlayHome` 按手绘稿重新组织为顶部状态条、中央章节卡、两侧快捷栏、底部 Tab 四层结构；其余子页面通过统一标题栏和卡片样式逐步翻新。

**Tech Stack：** React 19 + TypeScript + Tailwind CSS 3 + Lucide React + Zustand + React Router DOM v7（HashRouter）。不引入新依赖。

## Global Constraints

- 不引入外部图片资源或 UI 组件库。
- 所有图标使用 `lucide-react`，头像使用 emoji。
- 保持现有 `profileStore`、`gameStore`、`economyStore`、路由结构和数据模型不变。
- 所有样式改动优先使用 Tailwind 工具类，复杂动画/背景使用 `src/styles/index.css` 的 `@layer components`。
- 页面以手机竖屏为主，最大宽度 `max-w-md` 居中。
- 每次任务完成后运行 `npm run test:types` 与 `npm test -- --run` 确保无回归。
- 频繁提交，每个任务独立 commit。

---

## File Structure

| 文件/目录 | 用途 |
|---|---|
| `src/styles/index.css` | 新增营地背景、篝火/帐篷动画、卡片/按钮组件样式。 |
| `tailwind.config.ts` | 新增/复用动画 keyframes（`campfireFlicker`、`tentSway`）。 |
| `src/components/play/AvatarBadge.tsx` | 圆形 emoji 头像 + 等级角标。 |
| `src/components/play/StatusCapsule.tsx` | 顶部状态胶囊（图标 + 数值）。 |
| `src/components/play/IconButton.tsx` | 两侧 48px 圆形图标按钮。 |
| `src/components/play/ChapterNode.tsx` | 三科学科节点（已完成/当前/锁定）。 |
| `src/components/play/BottomTabBar.tsx` | 底部 4 Tab 导航。 |
| `src/components/play/ProfileModal.tsx` | 头像/昵称设置弹窗。 |
| `src/components/play/PageHeader.tsx` | 子页面统一标题栏（返回 + 标题 + 状态胶囊）。 |
| `src/pages/PlayHome.tsx` | 首页完全重写为新布局。 |
| `src/components/layout/PlayLayout.tsx` | 统一背景、挂载 BottomTabBar、处理安全区。 |
| `src/pages/Shop.tsx` | 商城页面按新风格翻新。 |
| `src/pages/Arcade.tsx` | 副本页面翻新。 |
| `src/pages/WrongQuestions.tsx` | 错题本翻新。 |
| `src/pages/Battle.tsx` | 战斗页面视觉翻新（HP 条、题目卡片、反馈动画）。 |
| `src/pages/Achievements.tsx` | 成就页面翻新。 |
| `src/pages/DailyTasks.tsx` | 每日任务翻新。 |
| `src/pages/Lottery.tsx` | 抽奖页面翻新。 |
| `src/components/play/EquipmentPanel.tsx` | 装备面板翻新。 |
| `src/components/play/PetPanel.tsx` | 宠物面板翻新。 |
| `src/pages/RewardShop.tsx` | 奖励兑换页翻新。 |
| `tests/components/` | 新增可复用组件的单元测试。 |

---

## Task 1: 视觉基础 — 新增营地背景与动画

**Files:**
- Modify: `src/styles/index.css`
- Modify: `tailwind.config.ts`
- Test: 视觉回归通过浏览器手动检查，无新增单元测试。

**Interfaces:**
- Produces: `.scene-camp-home` CSS class；`animate-campfireFlicker`、`animate-tentSway` Tailwind 动画类。

- [ ] **Step 1: 在 `tailwind.config.ts` 新增动画 keyframes**

```ts
animation: {
  // ... existing animations
  campfireFlicker: 'campfireFlicker 1.5s ease-in-out infinite',
  tentSway: 'tentSway 4s ease-in-out infinite',
},
keyframes: {
  // ... existing keyframes
  campfireFlicker: {
    '0%, 100%': { opacity: '0.7', transform: 'scale(1)' },
    '50%': { opacity: '1', transform: 'scale(1.05)' },
  },
  tentSway: {
    '0%, 100%': { transform: 'rotate(-1deg)' },
    '50%': { transform: 'rotate(1deg)' },
  },
}
```

- [ ] **Step 2: 在 `src/styles/index.css` 添加营地背景组件类**

```css
.scene-camp-home {
  position: relative;
  background: linear-gradient(180deg, #e0f2fe 0%, #dcfce7 100%);
  overflow: hidden;
}

.scene-camp-home::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    radial-gradient(circle at 15% 85%, rgba(34, 197, 94, 0.08), transparent 30%),
    radial-gradient(circle at 85% 80%, rgba(59, 130, 246, 0.08), transparent 30%);
  pointer-events: none;
}

.camp-tent {
  position: absolute;
  bottom: 80px;
  left: 8%;
  width: 64px;
  height: 48px;
  background: #166534;
  clip-path: polygon(50% 0%, 0% 100%, 100% 100%);
  opacity: 0.25;
  animation: tentSway 4s ease-in-out infinite;
}

.camp-tree {
  position: absolute;
  bottom: 80px;
  width: 0;
  height: 0;
  border-left: 16px solid transparent;
  border-right: 16px solid transparent;
  border-bottom: 56px solid #15803d;
  opacity: 0.2;
  animation: tentSway 5s ease-in-out infinite reverse;
}

.camp-fire {
  position: absolute;
  bottom: 78px;
  left: 50%;
  transform: translateX(-50%);
  width: 24px;
  height: 28px;
  background: radial-gradient(circle at 50% 80%, #f97316, #fbbf24 60%, transparent 70%);
  border-radius: 50% 50% 50% 50% / 60% 60% 40% 40%;
  opacity: 0.6;
  animation: campfireFlicker 1.5s ease-in-out infinite;
}
```

- [ ] **Step 3: 运行类型检查**

Run: `npm run test:types`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add tailwind.config.ts src/styles/index.css
git commit -m "style(camp): add camp home background and silhouette animations"
```

---

## Task 2: 可复用组件 — AvatarBadge、StatusCapsule、IconButton

**Files:**
- Create: `src/components/play/AvatarBadge.tsx`
- Create: `src/components/play/StatusCapsule.tsx`
- Create: `src/components/play/IconButton.tsx`
- Create: `tests/components/AvatarBadge.test.tsx`
- Create: `tests/components/StatusCapsule.test.tsx`
- Create: `tests/components/IconButton.test.tsx`

**Interfaces:**
- Produces:
  - `<AvatarBadge emoji="🧒" level={5} onClick={() => {}} />`
  - `<StatusCapsule icon={Zap} value="5/10" colorClass="text-yellow-500" />`
  - `<IconButton icon={Sword} label="装备" onClick={() => {}} />`

- [ ] **Step 1: 实现 `AvatarBadge.tsx`**

```tsx
interface AvatarBadgeProps {
  emoji: string;
  level: number;
  onClick?: () => void;
  className?: string;
}

export function AvatarBadge({ emoji, level, onClick, className }: AvatarBadgeProps) {
  return (
    <button
      onClick={onClick}
      className={[
        'relative flex h-12 w-12 shrink-0 items-center justify-center rounded-full bg-white text-2xl shadow-md ring-2 ring-white/60 active:scale-95',
        className ?? ''
      ].join(' ')}
    >
      {emoji}
      <span className="absolute -bottom-1 -right-1 flex h-5 min-w-[20px] items-center justify-center rounded-full bg-indigo-600 px-1 text-[10px] font-bold text-white ring-2 ring-white">
        {level}
      </span>
    </button>
  );
}

- [ ] **Step 2: 实现 `StatusCapsule.tsx`**

```tsx
import type { LucideIcon } from 'lucide-react';

interface StatusCapsuleProps {
  icon: LucideIcon;
  value: string | number;
  iconClass?: string;
  className?: string;
}

export function StatusCapsule({ icon: Icon, value, iconClass, className }: StatusCapsuleProps) {
  return (
    <div className={['flex items-center gap-1.5 rounded-full bg-white px-3 py-1.5 text-sm font-bold shadow-sm', className].join(' ')}>
      <Icon className={['h-4 w-4', iconClass ?? 'text-slate-500'].join(' ')} />
      <span className="text-slate-700">{value}</span>
    </div>
  );
}
```

- [ ] **Step 3: 实现 `IconButton.tsx`**

```tsx
import type { LucideIcon } from 'lucide-react';

interface IconButtonProps {
  icon: LucideIcon;
  label: string;
  onClick?: () => void;
  to?: string;
  badge?: number;
  colorClass?: string;
}

export function IconButton({ icon: Icon, label, onClick, badge, colorClass }: IconButtonProps) {
  return (
    <button
      onClick={onClick}
      title={label}
      className="group relative flex h-12 w-12 items-center justify-center rounded-full bg-white shadow-md transition hover:scale-110 active:scale-95"
    >
      <Icon className={['h-6 w-6', colorClass ?? 'text-slate-600'].join(' ')} />
      {badge !== undefined && badge > 0 && (
        <span className="absolute -right-1 -top-1 flex h-5 w-5 items-center justify-center rounded-full bg-red-500 text-[10px] font-bold text-white">
          {badge > 9 ? '9+' : badge}
        </span>
      )}
    </button>
  );
}
```

- [ ] **Step 4: 编写对应测试**

```tsx
// tests/components/AvatarBadge.test.tsx
import { render, screen } from '@testing-library/react';
import { describe, it, expect, vi } from 'vitest';
import { AvatarBadge } from '../../src/components/play/AvatarBadge';

describe('AvatarBadge', () => {
  it('renders emoji and level', () => {
    render(<AvatarBadge emoji="🧒" level={5} />);
    expect(screen.getByText('🧒')).toBeInTheDocument();
    expect(screen.getByText('5')).toBeInTheDocument();
  });

  it('calls onClick when clicked', () => {
    const handle = vi.fn();
    render(<AvatarBadge emoji="🧒" level={1} onClick={handle} />);
    screen.getByRole('button').click();
    expect(handle).toHaveBeenCalled();
  });
});
```

类似地编写 `StatusCapsule.test.tsx` 和 `IconButton.test.tsx`。

- [ ] **Step 5: 运行测试与类型检查**

Run: `npm run test:types && npm test -- --run`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add src/components/play/AvatarBadge.tsx src/components/play/StatusCapsule.tsx src/components/play/IconButton.tsx tests/components/
git commit -m "feat(ui): add AvatarBadge, StatusCapsule, IconButton components"
```

---

## Task 3: 可复用组件 — ChapterNode、BottomTabBar、ProfileModal

**Files:**
- Create: `src/components/play/ChapterNode.tsx`
- Create: `src/components/play/BottomTabBar.tsx`
- Create: `src/components/play/ProfileModal.tsx`
- Create: `tests/components/ChapterNode.test.tsx`
- Create: `tests/components/BottomTabBar.test.tsx`

**Interfaces:**
- Produces:
  - `<ChapterNode subject="math" status="current" />`
  - `<BottomTabBar active="camp" />`
  - `<ProfileModal open={true} onClose={() => {}} />`

- [ ] **Step 1: 实现 `ChapterNode.tsx`**

```tsx
import { Check } from 'lucide-react';
import type { Subject } from '../../types';

const SUBJECT_META: Record<Subject, { label: string; color: string; ring: string; bg: string }> = {
  chinese: { label: '语文', color: 'text-emerald-700', ring: 'ring-emerald-500', bg: 'bg-emerald-500' },
  math: { label: '数学', color: 'text-blue-700', ring: 'ring-blue-500', bg: 'bg-blue-500' },
  english: { label: '英语', color: 'text-amber-700', ring: 'ring-amber-500', bg: 'bg-amber-500' },
};

interface ChapterNodeProps {
  subject: Subject;
  status: 'passed' | 'current' | 'locked';
}

export function ChapterNode({ subject, status }: ChapterNodeProps) {
  const meta = SUBJECT_META[subject];
  if (status === 'locked') {
    return (
      <div className="flex h-14 w-14 items-center justify-center rounded-full bg-slate-200 ring-4 ring-slate-100">
        <span className="text-xl">🔒</span>
      </div>
    );
  }
  if (status === 'passed') {
    return (
      <div className={['flex h-14 w-14 items-center justify-center rounded-full text-white shadow-md', meta.bg].join(' ')}>
        <Check className="h-7 w-7" />
      </div>
    );
  }
  return (
    <div className={['flex h-14 w-14 animate-pulseGlow items-center justify-center rounded-full bg-white shadow-lg ring-4', meta.ring].join(' ')}>
      <span className={['text-sm font-bold', meta.color].join(' ')}>{meta.label}</span>
    </div>
  );
}
```

- [ ] **Step 2: 实现 `BottomTabBar.tsx`**

```tsx
import { Link, useLocation } from 'react-router-dom';
import { Tent, ShoppingBag, Gamepad2, BookOpen } from 'lucide-react';

const TABS = [
  { to: '/play', label: '营地', icon: Tent },
  { to: '/play/shop', label: '商城', icon: ShoppingBag },
  { to: '/play/arcade', label: '副本', icon: Gamepad2 },
  { to: '/play/wrong', label: '错题本', icon: BookOpen },
];

export function BottomTabBar() {
  const location = useLocation();
  return (
    <nav className="fixed bottom-0 left-0 right-0 z-50 flex justify-center pb-[env(safe-area-inset-bottom)]">
      <div className="mx-4 mb-3 flex w-full max-w-md items-center justify-around rounded-2xl bg-white/90 p-2 shadow-lg backdrop-blur-md">
        {TABS.map(tab => {
          const Icon = tab.icon;
          const active = location.pathname === tab.to;
          return (
            <Link
              key={tab.to}
              to={tab.to}
              className={[
                'flex flex-1 flex-col items-center gap-0.5 rounded-xl py-2 text-xs font-bold transition',
                active ? '-translate-y-1 bg-orange-500 text-white shadow-md' : 'text-slate-500 hover:bg-slate-100'
              ].join(' ')}
            >
              <Icon className="h-5 w-5" />
              <span>{tab.label}</span>
            </Link>
          );
        })}
      </div>
    </nav>
  );
}
```

- [ ] **Step 3: 实现 `ProfileModal.tsx`**

```tsx
import { useState } from 'react';
import { X } from 'lucide-react';
import { useProfileStore } from '../../stores/profileStore';

const AVATARS = ['🧒', '👧', '🧑', '👦', '👩', '🧑‍🚀', '🧑‍🎓', '🦁', '🐯', '🐼'];

interface ProfileModalProps {
  open: boolean;
  onClose: () => void;
}

export function ProfileModal({ open, onClose }: ProfileModalProps) {
  const profile = useProfileStore(state => state.profile);
  const updateProfile = useProfileStore(state => state.updateProfile);
  const [nickname, setNickname] = useState(profile?.nickname ?? '');

  if (!open || !profile) return null;

  return (
    <div className="fixed inset-0 z-[60] flex items-center justify-center bg-black/50 p-4">
      <div className="w-full max-w-sm rounded-2xl bg-white p-5 shadow-xl">
        <div className="mb-4 flex items-center justify-between">
          <h2 className="text-lg font-bold text-slate-800">个人设置</h2>
          <button onClick={onClose} className="rounded-full p-1 hover:bg-slate-100"><X className="h-5 w-5" /></button>
        </div>
        <div className="mb-4">
          <label className="mb-2 block text-sm font-semibold text-slate-600">昵称</label>
          <input
            value={nickname}
            onChange={e => setNickname(e.target.value)}
            className="w-full rounded-xl border border-slate-300 px-3 py-2 text-sm outline-none focus:border-indigo-500"
            maxLength={12}
          />
        </div>
        <div className="mb-4">
          <label className="mb-2 block text-sm font-semibold text-slate-600">头像</label>
          <div className="grid grid-cols-5 gap-2">
            {AVATARS.map(emoji => (
              <button
                key={emoji}
                onClick={() => updateProfile({ avatar: emoji })}
                className={[
                  'flex h-10 items-center justify-center rounded-xl text-xl transition',
                  profile.avatar === emoji ? 'bg-indigo-100 ring-2 ring-indigo-500' : 'bg-slate-100 hover:bg-slate-200'
                ].join(' ')}
              >
                {emoji}
              </button>
            ))}
          </div>
        </div>
        <button
          onClick={() => { updateProfile({ nickname }); onClose(); }}
          className="w-full rounded-xl bg-indigo-600 py-2.5 font-bold text-white active:scale-95"
        >
          保存
        </button>
      </div>
    </div>
  );
}
```

> 注意：`profileStore` 中需要存在 `avatar` 字段和 `updateProfile` action；若不存在， plan 实施前先添加迁移（见 Task 9）。

- [ ] **Step 4: 编写测试**

```tsx
// tests/components/ChapterNode.test.tsx
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import { ChapterNode } from '../../src/components/play/ChapterNode';

describe('ChapterNode', () => {
  it('shows check for passed', () => {
    render(<ChapterNode subject="chinese" status="passed" />);
    expect(screen.getByRole('img', { hidden: true })).toBeInTheDocument();
  });

  it('shows lock for locked', () => {
    render(<ChapterNode subject="math" status="locked" />);
    expect(screen.getByText('🔒')).toBeInTheDocument();
  });
});
```

- [ ] **Step 5: 运行测试与类型检查**

Run: `npm run test:types && npm test -- --run`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add src/components/play/ChapterNode.tsx src/components/play/BottomTabBar.tsx src/components/play/ProfileModal.tsx tests/components/
git commit -m "feat(ui): add ChapterNode, BottomTabBar, ProfileModal components"
```

---

## Task 4: 改造 `PlayLayout` — 统一背景与挂载底部 Tab

**Files:**
- Modify: `src/components/layout/PlayLayout.tsx`
- Test: 手动验证底部 Tab 在四个一级页面可见，子页面隐藏；运行现有测试。

**Interfaces:**
- Consumes: `<BottomTabBar />` from Task 3.
- Produces: Layout with `scene-camp-home` background and conditional bottom tab.

- [ ] **Step 1: 修改 `PlayLayout.tsx`**

```tsx
import { Outlet, useLocation } from 'react-router-dom';
import { useEffect } from 'react';
import { useProfileStore } from '../../stores/profileStore';
import { useHealthGuard } from '../../hooks/useHealthGuard';
import { RestModeOverlay } from '../play/RestModeOverlay';
import { EyeCareModal } from '../play/EyeCareModal';
import { BottomTabBar } from '../play/BottomTabBar';

const TAB_ROUTES = ['/play', '/play/shop', '/play/arcade', '/play/wrong'];

export function PlayLayout() {
  const loadProfile = useProfileStore(state => state.loadProfile);
  const loaded = useProfileStore(state => state.loaded);
  const error = useProfileStore(state => state.error);
  const clearError = useProfileStore(state => state.clearError);
  const { isRestModeActive, showEyeCare, dismissEyeCare } = useHealthGuard();
  const location = useLocation();

  useEffect(() => { loadProfile(); }, [loadProfile]);

  if (!loaded) {
    return <div className="flex h-screen items-center justify-center">加载中...</div>;
  }

  if (error) { /* existing error UI */ }

  const showTab = TAB_ROUTES.includes(location.pathname);

  return (
    <div className="scene-camp-home relative flex min-h-screen flex-col">
      <RestModeOverlay isActive={isRestModeActive} />
      <EyeCareModal show={showEyeCare} onDismiss={dismissEyeCare} />
      <main className={['flex-1', showTab ? 'pb-24' : ''].join(' ')}>
        <Outlet />
      </main>
      {showTab && <BottomTabBar />}
    </div>
  );
}
```

- [ ] **Step 2: 移除子页面旧的全局 `学霸星球` 标题栏**

原 `PlayLayout` 中非首页会渲染 `header`。删除该 header，统一由每个子页面自己的 `PageHeader` 控制（见 Task 5）。

- [ ] **Step 3: 运行测试**

Run: `npm run test:types && npm test -- --run`
Expected: PASS（现有 snapshot 可能需更新，按提示处理）

- [ ] **Step 4: 提交**

```bash
git add src/components/layout/PlayLayout.tsx
git commit -m "feat(layout): apply camp background and mount BottomTabBar in PlayLayout"
```

---

## Task 5: 子页面统一标题栏 `PageHeader`

**Files:**
- Create: `src/components/play/PageHeader.tsx`
- Create: `tests/components/PageHeader.test.tsx`

**Interfaces:**
- Produces: `<PageHeader title="商城" showStats />`

- [ ] **Step 1: 实现 `PageHeader.tsx`**

```tsx
import { useNavigate } from 'react-router-dom';
import { ArrowLeft, Star, Zap } from 'lucide-react';
import { useProfileStore } from '../../stores/profileStore';
import { MAX_STAMINA } from '../../services/staminaLogic';
import { StatusCapsule } from './StatusCapsule';

interface PageHeaderProps {
  title: string;
  showStats?: boolean;
}

export function PageHeader({ title, showStats = true }: PageHeaderProps) {
  const navigate = useNavigate();
  const profile = useProfileStore(state => state.profile);

  return (
    <header className="sticky top-0 z-40 flex items-center justify-between bg-white/80 px-4 py-3 backdrop-blur-md">
      <button
        onClick={() => navigate('/play')}
        className="flex h-10 w-10 items-center justify-center rounded-full bg-white shadow-sm active:scale-95"
      >
        <ArrowLeft className="h-5 w-5 text-slate-600" />
      </button>
      <h1 className="text-lg font-bold text-slate-800">{title}</h1>
      {showStats && profile ? (
        <div className="flex items-center gap-2">
          <StatusCapsule icon={Star} value={profile.stars} iconClass="fill-yellow-400 text-yellow-400" />
          <StatusCapsule icon={Zap} value={`${profile.stamina}/${MAX_STAMINA}`} iconClass="fill-yellow-400 text-yellow-400" />
        </div>
      ) : (
        <div className="w-10" />
      )}
    </header>
  );
}
```

- [ ] **Step 2: 编写测试**

```tsx
// tests/components/PageHeader.test.tsx
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { describe, it, expect } from 'vitest';
import { PageHeader } from '../../src/components/play/PageHeader';

describe('PageHeader', () => {
  it('renders title and back button', () => {
    render(<MemoryRouter><PageHeader title="商城" /></MemoryRouter>);
    expect(screen.getByText('商城')).toBeInTheDocument();
    expect(screen.getByRole('button', { name: '' })).toBeInTheDocument();
  });
});
```

- [ ] **Step 3: 运行测试**

Run: `npm run test:types && npm test -- --run`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add src/components/play/PageHeader.tsx tests/components/PageHeader.test.tsx
git commit -m "feat(ui): add PageHeader component for child pages"
```

---

## Task 6: 重写 `PlayHome` 首页

**Files:**
- Modify: `src/pages/PlayHome.tsx`
- Modify: `src/styles/index.css`（如需新增 `.chapter-card` 等辅助类）
- Test: 更新 `tests/pages/PlayHome.test.tsx` 或新增首页渲染测试。

**Interfaces:**
- Consumes: `AvatarBadge`, `StatusCapsule`, `ChapterNode`, `IconButton`, `ProfileModal`, `BottomTabBar`.
- Produces: New `PlayHome` component.

- [ ] **Step 1: 重写 `PlayHome.tsx`**

保留所有数据加载逻辑（`loadProgress`, `loadInventory`, `refreshStaminaNow`, `loadParentData`），仅替换 JSX 结构。

新结构要点：

```tsx
export function PlayHome() {
  // existing hooks and data loading
  const [profileOpen, setProfileOpen] = useState(false);
  const navigate = useNavigate();

  // existing derived state: refreshedProfile, currentLevel, progress, etc.

  const currentSubject = useMemo<Subject>(() => {
    const p = progress.find(p => p.subject === 'chinese' && p.levelNumber === currentLevel);
    if (p?.status === 'unlocked') return 'chinese';
    const m = progress.find(p => p.subject === 'math' && p.levelNumber === currentLevel);
    if (m?.status === 'unlocked') return 'math';
    const e = progress.find(p => p.subject === 'english' && p.levelNumber === currentLevel);
    if (e?.status === 'unlocked') return 'english';
    return 'chinese';
  }, [progress, currentLevel]);

  const ctaMeta = SUBJECT_META[currentSubject];

  return (
    <div className="relative flex min-h-full flex-col p-4">
      {/* 背景营地装饰 */}
      <div className="camp-tent" />
      <div className="camp-tree" style={{ right: '12%', borderBottomColor: '#15803d' }} />
      <div className="camp-fire" />

      {/* 顶部状态条 */}
      <div className="relative z-10 flex items-center justify-between">
        <AvatarBadge
          emoji={refreshedProfile.avatar ?? '🧒'}
          level={refreshedProfile.level}
          onClick={() => setProfileOpen(true)}
        />
        <div className="flex items-center gap-2">
          <StatusCapsule icon={Zap} value={`${refreshedProfile.stamina}/${MAX_STAMINA}`} iconClass="fill-yellow-400 text-yellow-400" />
          <StatusCapsule icon={Star} value={refreshedProfile.stars} iconClass="fill-yellow-400 text-yellow-400" />
        </div>
      </div>

      {/* 中央章节卡 */}
      <div className="relative z-10 mx-auto mt-8 w-full max-w-sm rounded-3xl bg-white p-6 shadow-xl">
        <h2 className="text-center text-xl font-black text-slate-800">第 {currentLevel} 章</h2>
        <p className="mb-6 text-center text-sm text-slate-500">{chapterTopic}</p>
        <div className="relative flex items-center justify-between px-4">
          <div className="absolute left-8 right-8 top-1/2 -z-10 h-1 -translate-y-1/2 rounded-full bg-slate-200" />
          {(['chinese', 'math', 'english'] as Subject[]).map(subject => {
            const sp = progress.find(p => p.subject === subject && p.levelNumber === currentLevel);
            const status = sp?.status === 'passed' ? 'passed' : sp?.status === 'locked' ? 'locked' : 'current';
            return <ChapterNode key={subject} subject={subject} status={status} />;
          })}
        </div>
        <button
          onClick={() => navigate(`/play/battle/${currentSubject}/${currentLevel}`)}
          className={['mt-8 w-full rounded-2xl py-4 text-lg font-black text-white shadow-lg transition active:scale-95 bg-gradient-to-r', ctaMeta.bg].join(' ')}
        >
          ▶ 开始闯关
        </button>
      </div>

      {/* 两侧快捷入口 */}
      <div className="relative z-10 mt-8 flex flex-1 justify-between">
        <div className="flex flex-col gap-3">
          <IconButton icon={Sword} label="装备" onClick={() => navigate('/play/equipment')} />
          <IconButton icon={Cat} label="宠物" onClick={() => navigate('/play/pet')} />
          <IconButton icon={Trophy} label="成就" onClick={() => navigate('/play/achievements')} />
        </div>
        <div className="flex flex-col gap-3">
          <IconButton icon={Backpack} label="背包" onClick={() => navigate('/play/inventory')} badge={inventoryCount} />
          <IconButton icon={ClipboardList} label="任务" onClick={() => navigate('/play/tasks')} />
          <IconButton icon={Ticket} label="抽奖" onClick={() => navigate('/play/lottery')} />
          <IconButton icon={Settings} label="设置" onClick={() => setProfileOpen(true)} />
        </div>
      </div>

      <ProfileModal open={profileOpen} onClose={() => setProfileOpen(false)} />
    </div>
  );
}
```

> 需要确认 `lucide-react` 中是否有 `Sword`、`Cat`、`Backpack`、`Ticket` 图标；若不存在，使用功能相近的图标（如 `Swords`、`PawPrint`、`ShoppingBag`、`Gift`）。

- [ ] **Step 2: 更新测试**

```tsx
// tests/pages/PlayHome.test.tsx
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { describe, it, expect } from 'vitest';
import { PlayHome } from '../../src/pages/PlayHome';

describe('PlayHome', () => {
  it('renders start challenge button', () => {
    render(<MemoryRouter><PlayHome /></MemoryRouter>);
    expect(screen.getByText(/开始闯关/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 3: 运行测试与类型检查**

Run: `npm run test:types && npm test -- --run`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add src/pages/PlayHome.tsx tests/pages/PlayHome.test.tsx
git commit -m "feat(play-home): rewrite PlayHome with new camp layout"
```

---

## Task 7: 翻新子页面 — 商城、副本、错题本

**Files:**
- Modify: `src/pages/Shop.tsx`
- Modify: `src/pages/Arcade.tsx`
- Modify: `src/pages/WrongQuestions.tsx`

**Interfaces:**
- Consumes: `PageHeader`.

- [ ] **Step 1: 为 `Shop.tsx` 添加统一标题栏和卡片样式**

在页面最外层添加：

```tsx
<PageHeader title="商城" />
<div className="p-4">
  {/* existing shop content wrapped in .card */}
</div>
```

将商品项容器 class 改为 `card`（白底圆角卡片）。分类 Tab 改为圆角胶囊按钮。

- [ ] **Step 2: 为 `Arcade.tsx` 添加统一标题栏和卡片样式**

```tsx
<PageHeader title="副本乐园" />
<div className="p-4 grid gap-4">
  {games.map(game => (
    <div key={game.id} className="card flex items-center gap-4">
      <div className="...">{game.icon}</div>
      <div className="flex-1">
        <h3 className="font-bold">{game.name}</h3>
        <p className="text-sm text-slate-500">{game.desc}</p>
      </div>
      <button className="btn-primary">开始</button>
    </div>
  ))}
</div>
```

- [ ] **Step 3: 为 `WrongQuestions.tsx` 添加统一标题栏和卡片样式**

顶部添加分科筛选胶囊：

```tsx
<div className="mb-4 flex gap-2">
  {(['all', 'chinese', 'math', 'english'] as const).map(s => (
    <button
      key={s}
      className={['rounded-full px-3 py-1 text-sm font-bold', filter === s ? 'bg-indigo-600 text-white' : 'bg-white text-slate-600'].join(' ')}
      onClick={() => setFilter(s)}
    >
      {s === 'all' ? '全部' : SUBJECT_LABELS[s]}
    </button>
  ))}
</div>
```

错题卡片使用 `card` 样式。

- [ ] **Step 4: 运行测试与类型检查**

Run: `npm run test:types && npm test -- --run`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/pages/Shop.tsx src/pages/Arcade.tsx src/pages/WrongQuestions.tsx
git commit -m "feat(ui): restyle Shop, Arcade, WrongQuestions with PageHeader and cards"
```

---

## Task 8: 翻新子页面 — 战斗、成就、任务、抽奖

**Files:**
- Modify: `src/pages/Battle.tsx`
- Modify: `src/pages/Achievements.tsx`
- Modify: `src/pages/DailyTasks.tsx`
- Modify: `src/pages/Lottery.tsx`

- [ ] **Step 1: 战斗页面 `Battle.tsx`**

保留状态逻辑，视觉调整：

- 背景使用对应学科场景类（`scene-chinese`/`scene-math`/`scene-english`）。
- HP 条改为圆角胶囊：`<div className="h-4 w-full rounded-full bg-slate-200"><div className="h-full rounded-full bg-gradient-to-r ..." /></div>`。
- 题目卡片使用 `card` 大圆角。
- 选项按钮使用 `btn-primary` / `btn-secondary` 变体。
- 正确/错误反馈使用 `animate-bounceShort` / `animate-shake`。

- [ ] **Step 2: 成就、任务、抽奖**

统一添加 `PageHeader`，内容区域使用 `card` 或 `grid` + `card`。

- [ ] **Step 3: 运行测试与类型检查**

Run: `npm run test:types && npm test -- --run`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add src/pages/Battle.tsx src/pages/Achievements.tsx src/pages/DailyTasks.tsx src/pages/Lottery.tsx
git commit -m "feat(ui): restyle Battle, Achievements, DailyTasks, Lottery"
```

---

## Task 9: 翻新子页面 — 装备、宠物、奖励兑换 + 档案字段迁移

**Files:**
- Modify: `src/components/play/EquipmentPanel.tsx`
- Modify: `src/components/play/PetPanel.tsx`
- Modify: `src/pages/RewardShop.tsx`
- Modify: `src/stores/profileStore.ts`（若缺少 `avatar` 字段）
- Modify: `src/types/index.ts`（若 `Profile` 类型缺少 `avatar`）

- [ ] **Step 1: 检查并补充 `avatar` 字段**

如果 `Profile` 类型和 `profileStore` 初始值中没有 `avatar`，添加：

```ts
// src/types/index.ts
export interface Profile {
  // ...existing fields
  avatar?: string;
}
```

```ts
// src/stores/profileStore.ts 初始化
avatar: '🧒',
```

- [ ] **Step 2: 装备、宠物、奖励兑换页面翻新**

统一添加 `PageHeader`，使用 `card` 包裹内容块，按钮使用 `btn-primary`。

- [ ] **Step 3: 运行测试与类型检查**

Run: `npm run test:types && npm test -- --run`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add src/components/play/EquipmentPanel.tsx src/components/play/PetPanel.tsx src/pages/RewardShop.tsx src/stores/profileStore.ts src/types/index.ts
git commit -m "feat(ui): restyle Equipment, Pet, RewardShop and add avatar field"
```

---

## Task 10: 清理旧样式与统一兜底

**Files:**
- Modify: `src/styles/index.css`
- Modify: `src/router.tsx`（如需添加 `/play/inventory` 路由）

- [ ] **Step 1: 移除或标记弃用不再使用的类**

例如 `.scene-planet-home` 首页不再使用，但保留以防其他页面引用。若确认无引用，可删除。

- [ ] **Step 2: 若背包入口指向 `/play/inventory`，确认路由存在**

当前路由中没有 `/play/inventory`。背包内容可由商城的“已拥有”Tab 或装备面板的背包列表承担。若需要独立背包页，新增路由和页面；否则把背包按钮指向 `/play/equipment`。

- [ ] **Step 3: 全量验证**

Run: `npm run test:types && npm test -- --run && npm run build`
Expected: PASS

- [ ] **Step 4: 提交**

```bash
git add .
git commit -m "chore(ui): clean up legacy styles and finalize child UI refactor"
```

---

## Spec Coverage Checklist

| 设计文档要求 | 对应任务 |
|---|---|
| 明亮渐变天空/草地背景 | Task 1 |
| 营地元素剪影 + 动画 | Task 1 |
| 顶部头像徽章 + 状态胶囊 | Task 2, Task 6 |
| 中央章节卡 + 三科节点 | Task 3 (ChapterNode), Task 6 |
| “开始闯关”学科动态色按钮 | Task 6 |
| 两侧圆形快捷入口 | Task 2 (IconButton), Task 6 |
| 底部 4 Tab 导航 | Task 3 (BottomTabBar), Task 4 |
| 子页面统一标题栏 + 卡片 | Task 5, Task 7, Task 8, Task 9 |
| 头像/昵称设置弹窗 | Task 3 (ProfileModal), Task 9 |
| 战斗/商城/副本/错题本等所有子页面翻新 | Task 7, Task 8, Task 9 |

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-30-孩子端UI重构-plan.md`.

Two execution options:

1. **Subagent-Driven (recommended)** - Dispatch a fresh subagent per task, review between tasks, fast iteration.
2. **Inline Execution** - Execute tasks in this session using `executing-plans`, batch execution with checkpoints.

Which approach would you like?
